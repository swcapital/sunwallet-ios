import SwiftUI

struct Thumb: View {
    var body: some View {
        Rectangle()
            .frame(width: 50, height: 4)
            .foregroundColor(Color.gray)
            .cornerRadius(6)
    }
}
